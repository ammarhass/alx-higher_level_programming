#!/usr/bin/python3
"""function to check the class type"""


def is_same_class(obj, a_class):
    """function to checks if an object is an instance of a given class"""

    if (type(obj) == a_class):
        return (True)
    return (False)
